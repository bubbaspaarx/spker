function refreshMessages() {
  const messages = document.getElementById('message-list')
  messages.innerHTML = ""
  <% @messages.each do |message| %>
    newMessage = '<%= render 'shared/message', {message: message} %>'
    messages.insertAdjacentHTML('afterbegin', newMessage)
  <% end %>
}

refreshMessages()
