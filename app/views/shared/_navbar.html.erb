<% unless user_signed_in? %>
<%= render 'shared/login_modal' %>
<%= render 'shared/signup_modal' %>
<% end %>
<div class="navbar-wagon">
  <!-- Logo -->
  <%= link_to root_path, class: "navbar-wagon-brand" do %>

  <%= image_tag "spkr orange.png", height: 50 %>

  <% end %>

  <!-- Right Navigation -->
  <div class="navbar-wagon-right hidden-xs hidden-sm">

    <% if user_signed_in? %>

    <!-- Links when logged in -->
    <% unless current_user.is_speaker %>
    <%= link_to "Become a speaker", user_speaker_new_path(current_user), class: "navbar-wagon-item navbar-wagon-link" %>
    <% end %>
    <%= link_to "Create an event", new_user_event_path(current_user), class: "navbar-wagon-item navbar-wagon-link" %>

    <% if current_user == true %>
    <%= link_to "Dashboard", dashboard_path(current_user), class: "navbar-wagon-item navbar-wagon-link" %>
    <% end %>


    <!-- Avatar with dropdown menu -->
    <div class="navbar-wagon-item">
      <div class="dropdown">
        <% image = current_user.photo.url ? current_user.photo : "http://res.cloudinary.com/montycat/image/upload/v1520001176/profile-placeholder.png" %>
        <%= cl_image_tag image, class: "avatar dropdown-toggle", id: "navbar-wagon-menu", "data-toggle" => "dropdown" %>
        <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
          <li>
            <%= link_to dashboard_path(current_user) do %>
            <i class="fa fa-user"></i> <%= t(".profile", default: "Profile") %>
            <% end %>
          </li>
          <li>
            <%= link_to inbox_path(current_user), class: "navbar-wagon-item navbar-wagon-link" do %>
            <i class="far fa-envelope"></i> <%= t(".inbox", default: "Inbox") %>
            <% end %>
          </li>
          <li>
            <%= link_to destroy_user_session_path, method: :delete, class: 'navbar-wagon-item navbar-wagon-link' do %>
            <i class="fa fa-sign-out-alt "></i>  <%= t(".sign_out", default: "Log out") %>
            <% end %>
          </li>
        </ul>
      </div>
    </div>
    <% else %>
    <!-- Login link (when logged out) -->
    <button id='login-btn' type="button" data-button="login-form" data-toggle="modal" data-target="#login_modal">Log in</button>
    <button id='signup-btn' type="button" data-button="signup-form" data-toggle="modal" data-target="#signup_modal">Sign up</button>
    <% end %>
  </div>

  <!-- Dropdown list appearing on mobile only -->
  <% if user_signed_in? %>
  <div class="navbar-wagon-item hidden-md hidden-lg">
    <div class="dropdown">
      <i class="fa fa-bars dropdown-toggle" data-toggle="dropdown"></i>
      <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
        <li>
          <%= link_to "Create an event", new_user_event_path(current_user), class: "navbar-wagon-item navbar-wagon-link" %>
        </li>
        <% if current_user.is_speaker == true %>
        <li>
          <%= link_to "Dashboard", user_path(current_user), class: "navbar-wagon-item navbar-wagon-link" %>
        </li>
        <% end %>
        <li>
          <%= link_to dashboard_path(current_user, active_tab: 'profile')  do %>
          <i class="fa fa-user"></i> <%= t(".profile", default: "Profile") %>
          <% end %>
        </li>
        <li>
          <%= link_to destroy_user_session_path, method: :delete do %>
          <i class="fa fa-sign-out-alt"></i>  <%= t(".sign_out", default: "Log out") %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
  <% else %>
  <div class="navbar-wagon-item hidden-md hidden-lg">
    <div class="dropdown">
      <i class="fa fa-bars dropdown-toggle" data-toggle="dropdown"></i>
      <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
        <li>
          <%= link_to "", data: { button: "signup-form", toggle: "modal", target: "#signup_modal" } do %>
          <%= t(".sign_up", default: "Sign Up") %>
          <% end %>
        </li>
        <li>
          <%= link_to "", data: { button: "login-form", toggle: "modal", target: "#login_modal" } do %>
          <%= t(".sign_in", default: "Log In") %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
  <% end %>
</div>
