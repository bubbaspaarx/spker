<%= simple_form_for(@user, html: { method: :put }) do |f| %>
    <%= f.input :photo, as: :hidden, class: 'upload-button' %>
    <%= f.input :photo_cache, as: :hidden, class: 'file-name' %>
    <%= f.button :submit, "+", class: 'load-image' %>
<% end %>

