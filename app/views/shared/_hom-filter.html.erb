<form class="filter-searchform2" action="speakers/">
  <div class="filters2 input-group">
    <input type="search" class="filter-searchbar2" name="cost" placeholder="Fee per hour" value="<%= params[:cost] %>">
    <select name="category" class ="filter-searchbar2" placeholder="Category">
      <option selected disabled>Category</option>
      <% Category.all.each do |category| %>
        <option value="<%= category.name %>" <%= "#{'selected' if category.name == params[:category]}" %> ><%= category.name %></option>
      <% end %>
    </select>
    <input type="search" class="filter-searchbar2" name="name" placeholder="Name" value="<%= params[:name] %>">
    <input type="search" id="filter-location" class="filter-searchbar2" name="location" placeholder="Location" value="<%= params[:location] %>">
    <button type="submit" class="btn btn-filter2" placeholder="Search">Search</button>
  </div>
</form>
