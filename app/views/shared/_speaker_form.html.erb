<%= simple_form_for(user, url: url) do |f| %>
  <%= f.input :postcode %>
  <%= f.input :travel_distance %>
  <%= f.input :address %>
  <%= f.input :cost %>
  <%= f.button :submit, class: 'btn btn-primary', value: 'Become a speaker' %>
<% end %>
